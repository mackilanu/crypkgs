[package]
name = "ncurses"
version = "6.5"
description = "The Ncurses package contains libraries for terminal-independent handling of character screens. "
license = "MIT"
arch="*"
dependencies = []
maintainer = "Marcus Andersson <marcus@unifun.se>"
source_location="https://invisible-mirror.net/archives/ncurses/ncurses-6.5.tar.gz"

build = [
    "BUILD",
]
